// govee-toolkit — Node SDK entry point.
//
// TODO: public API. Planned modules:
//   discovery.ts     — multicast scan + persistent cache
//   modes/lan.ts     — reused UDP socket, fire-and-verify
//   modes/ble.ts     — BLE mode (opt-in)
//   modes/cloud.ts   — Cloud mode (opt-in, throttled)
//   selector.ts      — per-device enabled modes + preference order
//   breaker.ts       — per-device, per-mode circuit breaker
//   registry.ts      — loads devices/*.yaml

export {};
