# Changelog

All notable changes to `govee-toolkit` (Node.js) are documented in this file.

The format is based on
[Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/), and this project
adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

Nothing has been released, and the package has no implementation yet: the
directory holds the manifest, a TypeScript config and an empty entry point. The
napi-rs binding over the Rust core lands with milestone 6 of
[`../../docs/roadmap.md`](../../docs/roadmap.md).

### Added

- Package scaffolding: `package.json` reserving the name `govee-toolkit` (ESM,
  no dependencies), `tsconfig.json`, and an empty `src/index.ts`.
