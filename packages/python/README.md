# govee-toolkit (Python)

Python SDK. `lan` mode by default; `ble` and `cloud` modes are opt-in, enabled
per device by the user — see [`../../docs/modes.md`](../../docs/modes.md).

Reads the shared device database in [`../../devices/`](../../devices/) — no
protocol logic is duplicated here.

<!-- TODO: install + usage once implemented -->
