# govee-toolkit (PHP)

PHP SDK, PSR-4. Same mode model as the other SDKs (see
[`../../docs/modes.md`](../../docs/modes.md)). Ported from the Python and Node
reference implementations — port only once those two are stable.

Reads the shared device database in [`../../devices/`](../../devices/).

<!-- TODO: install + usage once implemented -->
