# Homebridge plugin

Node plugin exposing Govee devices to HomeKit, consuming `packages/node`.

Comes after the [Matter bridge](../matter/) and the Home Assistant integration.

Start only once `packages/node` is stable.

<!-- TODO: installation + config.json schema -->
