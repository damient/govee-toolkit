{
  "intro": "Every command the toolkit carries, with the same action in each language. Python and Node.js bind to the same core.",
  "groups": [
    {
      "id": "find",
      "title": "Find devices",
      "entries": [
        {
          "id": "scan",
          "title": "scan",
          "summary": "Look for devices and report what answers.",
          "detail": "The scan runs in every enabled mode. `scan_on` restricts it to the modes you name, and enables none that the configuration leaves out.",
          "examples": {
            "cli": "govee scan\ngovee scan --mode lan",
            "rust": "let devices = govee.scan().await?;\nlet lan_only = govee.scan_on(&[Mode::Lan]).await?;",
            "python": "devices = await govee.scan()\nlan_only = await govee.scan_on([\"lan\"])",
            "node": "const devices = await govee.scan()\nconst lanOnly = await govee.scanOn(['lan'])"
          }
        },
        {
          "id": "devices",
          "title": "devices",
          "summary": "List the devices already known. It touches no network.",
          "detail": "The `lan` cache answers from disk, so a device found once costs no scan on the next run.",
          "examples": {
            "cli": "govee devices",
            "rust": "for device in govee.devices() {\n    println!(\"{}\", device.id());\n}",
            "python": "for device in govee.devices():\n    print(device.id)",
            "node": "for (const device of govee.devices()) {\n  console.log(device.id)\n}"
          }
        },
        {
          "id": "describe",
          "title": "describe",
          "summary": "Report what the device file declares for a model.",
          "detail": "It reads no hardware. Use it to see the range of every argument before you send a value.",
          "examples": {
            "cli": "govee describe DEVICE",
            "rust": "let spec = govee.device(&id).spec()?;",
            "python": "spec = govee.device(id).spec()",
            "node": "const spec = govee.device(id).spec()"
          }
        },
        {
          "id": "status",
          "title": "status",
          "roles": ["status"],
          "action": { "order": 80, "title": "Read the state back" },
          "summary": "Ask the device for its current state.",
          "detail": "`last_status` returns the state already known and sends nothing.",
          "examples": {
            "cli": "govee status DEVICE",
            "rust": "let state = device.status().await?;\nlet cached = device.last_status();",
            "python": "state = await device.status()\ncached = device.last_status()",
            "node": "const state = await device.status()\nconst cached = device.lastStatus()"
          }
        },
        {
          "id": "watch",
          "title": "watch",
          "summary": "Follow events: a device appears, a mode changes health.",
          "examples": {
            "cli": "govee watch\ngovee watch --json",
            "rust": "let mut events = govee.events();\nwhile let Ok(event) = events.recv().await {\n    println!(\"{event:?}\");\n}",
            "python": "async for event in govee.events():\n    print(event)",
            "node": "for await (const event of govee.events()) {\n  console.log(event)\n}"
          }
        },
        {
          "id": "doctor",
          "title": "doctor",
          "summary": "Report everything wrong with the configuration.",
          "detail": "Run it first when a device does not answer.",
          "examples": {
            "cli": "govee doctor",
            "rust": "for problem in govee.problems() {\n    eprintln!(\"{problem}\");\n}",
            "python": "for problem in govee.problems():\n    print(problem)",
            "node": "for (const problem of govee.problems()) {\n  console.error(problem)\n}"
          }
        }
      ]
    },
    {
      "id": "control",
      "title": "Control a light",
      "entries": [
        {
          "id": "power",
          "title": "power",
          "roles": ["power"],
          "action": { "order": 10, "title": "Turn it on and off" },
          "summary": "Turn the device on or off.",
          "examples": {
            "cli": "govee on DEVICE\ngovee off DEVICE",
            "rust": "device.power(true).await?;",
            "python": "await device.power(True)",
            "node": "await device.power(true)"
          }
        },
        {
          "id": "brightness",
          "title": "brightness",
          "roles": ["brightness"],
          "args": { "level": "brightness" },
          "values": { "level": 40 },
          "action": { "order": 20, "title": "Set the brightness" },
          "summary": "Set the brightness of the whole device.",
          "detail": "The device file carries the range. A value outside it is an error, never a clamp. On a verified model `0` is clamped by the firmware to `1` and does not turn the device off.",
          "examples": {
            "cli": "govee brightness DEVICE {level}",
            "rust": "device.brightness({level}).await?;",
            "python": "await device.brightness({level})",
            "node": "await device.brightness({level})"
          }
        },
        {
          "id": "color",
          "title": "color",
          "roles": ["color"],
          "action": { "order": 30, "title": "Set one color" },
          "summary": "Set one color for the whole device.",
          "examples": {
            "cli": "govee color DEVICE \"#ff3d00\"",
            "rust": "device.color([255, 61, 0]).await?;",
            "python": "await device.color((255, 61, 0))",
            "node": "await device.color([255, 61, 0])"
          }
        },
        {
          "id": "color-temp",
          "title": "color_temp",
          "roles": ["color_temp"],
          "args": { "kelvin": "color_temp" },
          "values": { "kelvin": 4000 },
          "action": {
            "order": 40,
            "title": "Set the white temperature",
            "summary": "White and color are mutually exclusive: this ends the color the device shows."
          },
          "summary": "Set the white temperature, in kelvin.",
          "detail": "It ends the color the device showed: white and color are two states that exclude each other. Where the mode carries the RGB rendering in the same frame, the core computes it and sends both.",
          "examples": {
            "cli": "govee colortemp DEVICE {kelvin}",
            "rust": "device.color_temp({kelvin}).await?;",
            "python": "await device.color_temp({kelvin})",
            "node": "await device.colorTemp({kelvin})"
          }
        },
        {
          "id": "segment",
          "title": "segment",
          "roles": ["segment_color", "segment_color_masked"],
          "action": { "order": 50, "title": "Paint the zones", "segments": true },
          "summary": "Paint the zones.",
          "detail": "One color fills every zone. A list of colors states one zone each, which is how a mode that addresses every LED is painted pixel by pixel: ask for the native resolution and pass that many colors. With a list of zones, the zones you name take the one color and the rest stay as they are; that needs a mode that paints by mask. A count the unit renders as a smaller one is refused, and the message names the counts it refines at.",
          "examples": {
            "cli": "govee segment DEVICE \"#ff3d00\"",
            "rust": "device.segment(&Paint {\n    zones: None,\n    colors: &frame,\n    resolution: Resolution::default(),\n    gradient: false,\n}).await?;",
            "python": "await device.segment(colors=frame)",
            "node": "await device.segment(frame)"
          },
          "adds": [
            {
              "needs": ["segment_color_masked"],
              "examples": {
                "cli": "govee segment DEVICE --zones 0,1,2 \"#ff3d00\""
              }
            },
            {
              "needs": ["native_pixels"],
              "examples": {
                "cli": "# One color per LED, in zone order. `describe` reports how many.\ngovee segment DEVICE --resolution native \"#ff3d00,#00a3ff,#000000,...\"\n# The same list, from a pipe or a file.\necho \"#ff3d00,#00a3ff,#000000,...\" | govee segment DEVICE --resolution native -",
                "rust": "device.segment(&Paint {\n    zones: None,\n    colors: &frame,\n    resolution: Resolution::Native,\n    gradient: false,\n}).await?;",
                "python": "await device.segment(colors=frame, resolution=\"native\")",
                "node": "await device.segment(frame, null, \"native\")"
              }
            }
          ]
        },
        {
          "id": "gradient",
          "title": "gradient",
          "roles": ["segment_gradient"],
          "action": {
            "order": 60,
            "title": "Interpolate between the zones",
            "summary": "The interpolation wraps from the last zone back to the first."
          },
          "summary": "Set whether the firmware interpolates between zones, without painting.",
          "detail": "The interpolation wraps from the last zone back to the first, so one lit zone at one end also lights the other. It is refused over a mode that carries the setting inside its painting frame: nothing there holds what the device shows, so the colors cannot be repainted under the other setting. Pass `--gradient` to `segment` over such a mode, which sets both at once.",
          "examples": {
            "cli": "govee gradient DEVICE on",
            "rust": "device.gradient(true).await?;",
            "python": "await device.gradient(True)",
            "node": "await device.gradient(true)"
          }
        },
        {
          "id": "music",
          "title": "music",
          "modes": ["ble", "cloud"],
          "roles": ["music"],
          "args": { "effect": "effect", "sensitivity": "sensitivity" },
          "values": { "effect": 3, "sensitivity": 60 },
          "action": {
            "order": 70,
            "title": "Play a music effect",
            "summary": "The device listens on its own microphone."
          },
          "summary": "Play an effect the device renders from its own microphone.",
          "detail": "The device listens and nothing streams from your host. The effect identifiers belong to the mode: the same number names another effect over another mode, and `lan` carries no music command at all. Nothing stops the effect — set a color, a temperature or the power to end it.",
          "examples": {
            "cli": "govee music DEVICE {effect} --sensitivity {sensitivity}",
            "rust": "device.music(&Music {\n    effect: {effect},\n    sensitivity: {sensitivity},\n    soft: false,\n    color: None,\n}).await?;",
            "python": "await device.music(effect={effect}, sensitivity={sensitivity})",
            "node": "await device.music({effect}, {sensitivity})"
          }
        }
      ]
    },
    {
      "id": "stream",
      "title": "Animate the segments",
      "modes": ["lan", "ble"],
      "entries": [
        {
          "id": "open-stream",
          "title": "open_stream",
          "summary": "Open the segment channel and keep it armed.",
          "detail": "The stream reports the zone count and the frame rate measured on the unit. It stays armed until you close it, and the colors end with it.",
          "examples": {
            "cli": "# frames.txt: one frame per line, one color per zone.\ngovee stream DEVICE --resolution native < frames.txt",
            "rust": "let stream = device.open_stream(StreamOptions::default()).await?;\nprintln!(\"{} zones at {} Hz\", stream.zones(), stream.rate_hz());",
            "python": "stream = await device.open_stream()\nprint(stream.zones, stream.rate_hz)",
            "node": "const stream = await device.openStream()\nconsole.log(stream.zones, stream.rateHz)"
          }
        },
        {
          "id": "set-zone",
          "title": "set_zone",
          "summary": "Set one zone of the stream.",
          "examples": {
            "cli": "# One frame per line, one color per zone.\necho \"000000,ff3d00,000000\" | govee stream DEVICE --resolution 3",
            "rust": "stream.set_zone(7, [255, 61, 0])?;",
            "python": "stream.set_zone(7, (255, 61, 0))",
            "node": "stream.setZone(7, [255, 61, 0])"
          }
        },
        {
          "id": "set-all",
          "title": "set_all, fill and clear",
          "summary": "Set every zone at once.",
          "detail": "`set_all` takes one color per zone. `fill` gives every zone the same color. `clear` sets them to black.",
          "examples": {
            "cli": "govee segment DEVICE \"#000000\"",
            "rust": "stream.set_all(&frame)?;\nstream.fill([255, 61, 0])?;\nstream.clear()?;",
            "python": "stream.set_all(frame)\nstream.fill((255, 61, 0))\nstream.clear()",
            "node": "stream.setAll(frame)\nstream.fill([255, 61, 0])\nstream.clear()"
          }
        },
        {
          "id": "close",
          "title": "close",
          "summary": "Close the channel.",
          "detail": "A frame that a newer one replaces before it goes out is counted in `frames_superseded`. Nothing else disarms the channel.",
          "examples": {
            "cli": "# The stream ends when standard input ends.",
            "rust": "println!(\"{} frames\", stream.frames_sent());\nstream.close().await?;",
            "python": "print(stream.frames_sent)\nawait stream.close()",
            "node": "console.log(stream.framesSent)\nawait stream.close()"
          }
        }
      ]
    },
    {
      "id": "raw",
      "title": "Raw commands and setup",
      "entries": [
        {
          "id": "send",
          "title": "send",
          "summary": "Send one entry of the device file, by name.",
          "detail": "It works for a model this build has never heard of. The entry declares the type of every argument, and the range stays the core's to check. `read` is the same for an entry that returns an answer.",
          "examples": {
            "cli": "govee send DEVICE brightness --arg level=40",
            "rust": "device.send(\"brightness\", &args).await?;\nlet reply = device.read(\"status\", &args).await?;",
            "python": "await device.send(\"brightness\", level=40)\nreply = await device.read(\"status\")",
            "node": "await device.send('brightness', { level: 40 })\nconst reply = await device.read('status')"
          }
        },
        {
          "id": "provision",
          "title": "provision_wifi",
          "modes": ["ble"],
          "roles": ["wifi_provision", "wifi_provision_with_api"],
          "action": { "order": 90, "title": "Put it on a Wi-Fi network" },
          "summary": "Put a new device on a Wi-Fi network, over Bluetooth.",
          "detail": "This is how a device out of the box becomes reachable over `lan`. The password travels in plaintext with no key exchange: anything in Bluetooth range while it runs can read it.",
          "examples": {
            "cli": "govee provision DEVICE --ssid my-network",
            "rust": "device.provision_wifi(&credentials).await?;",
            "python": "await device.provision_wifi(\"network name\", \"password\")",
            "node": "await device.provisionWifi(\"network name\", \"password\")"
          }
        },
        {
          "id": "modes",
          "title": "Choose a mode",
          "summary": "Restrict a command to one mode.",
          "detail": "It never enables a mode the configuration leaves out, and it never falls back. A device that no enabled mode reaches is an error.",
          "examples": {
            "cli": "govee on DEVICE --mode lan",
            "rust": "let mode = device.serving_mode()?;\nlet health = device.health(Mode::Lan);",
            "python": "mode = device.serving_mode()\nhealth = device.health(\"lan\")",
            "node": "const mode = device.servingMode()\nconst health = device.health('lan')"
          }
        }
      ]
    }
  ]
}
