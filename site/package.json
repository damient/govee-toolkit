{
  "name": "govee-toolkit-site",
  "private": true,
  "version": "0.0.0",
  "description": "The govee-toolkit website, built for GitHub Pages.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "catalog": "cargo run -p xtask -- catalog",
    "build": "node build.mjs",
    "dev": "BASE=/ node build.mjs --watch --serve",
    "serve": "BASE=/ node build.mjs --serve"
  },
  "dependencies": {
    "marked": "15.0.7"
  }
}
